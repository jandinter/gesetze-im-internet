name: scraper

on:
  schedule:
    - cron: '5 7 * * 3' # At 7:05am on Wednesday
    - cron: '5 7 * * *' # At 7:05am every day

jobs:
  scrape-legal-acts:
    runs-on: ubuntu-latest
    steps:
      - name: checkout
        uses: actions/checkout@v2

      - name: setup_ruby
        uses: ruby/setup-ruby@v1 # https://github.com/ruby/setup-ruby
        with:
          ruby-version: 3.0
          run: bundle install

      - name: run_scraper
        run: ruby scraper.rb

      - name: setup git config
        run: |
          # setup the username and email. I tend to use 'GitHub Actions Bot' with no email by default
          git config user.name "Scraper"
          git config user.email "<>"

      - name: commit
        run: |
          # Stage the file, commit and push
          git add gesetze/*
          git commit -m "Scraping of legal acts"
          git push origin main
      # - name: push
      #   uses: actions-x/commit@v2 # https://github.com/actions-x/commit
      #   with:
      #     name: Scraper
      #     branch: main
      #     files: gesetze/*
      #     message: Scraping of legal acts
